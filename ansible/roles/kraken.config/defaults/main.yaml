---
kraken_action: up
config_forced: "{{ config_forced }}"
config_base: "~/.kraken"
config_file_default: "{{ config_base | expanduser }}/config.yaml"
config_file: "{{ config_path | expanduser | default(config_file_default) }}"
