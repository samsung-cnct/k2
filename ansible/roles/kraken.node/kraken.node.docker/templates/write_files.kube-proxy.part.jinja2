- path: /etc/kubernetes/manifests/kube-proxy.yaml
  owner: root
  permissions: 0644
  encoding: "gz+b64"
  content: |
    {{ lookup('file', '{{ config_base }}/{{ item.0.name }}/node.{{ item.1.name }}.kube-proxy.yaml.manifest' | expanduser) | gz | b64encode }}
